{{!-- Build Options Template --}}

{{#each structures as |structure|}}
    <div class="sidebar" {{action 'build' structure.type}}>
        <img src="{{structure.name}}.gif">
        <span class="">Cost: ${{structure.price}}</span><br>
        <span class="">{{structure.description}}</span><br>
    </div>
{{/each}}
